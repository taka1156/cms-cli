{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Brite Configuration Schema",
  "type": "object",
  "properties": {
    "$schema": {
      "type": "string",
      "description": "The JSON schema version used for validation.",
      "default": "http://json-schema.org/draft-07/schema#"
    },
    "articleDir": {
      "type": "string",
      "description": "The directory path where your Markdown files (.md) are stored.",
      "default": "./articles"
    },
    "imageDir": {
      "type": "string",
      "description": "The directory path where your image files are stored.",
      "default": "./images"
    },
    "outputDir": {
      "type": "string",
      "description": "The directory path where all build outputs are exported.",
      "default": "./dist"
    },
    "cacheDir": {
      "type": "string",
      "description": "The directory path where your cache files are stored.",
      "default": "./cache"
    },
    "categories": {
      "type": "array",
      "description": "A whitelist of allowed categories. Unregistered categories found in markdown will be skipped.",
      "items": {
        "type": "string"
      },
      "uniqueItems": true
    },
    "tags": {
      "type": "array",
      "description": "A whitelist of allowed tags. Unregistered tags found in markdown will be skipped.",
      "items": {
        "type": "string"
      },
      "uniqueItems": true
    },
    "r2": {
      "type": "object",
      "description": "Cloudflare R2 configuration for image uploads.",
      "properties": {
        "bucketName": {
          "type": "string",
          "description": "R2 bucket name."
        },
        "baseUrl": {
          "type": "string",
          "description": "Base URL for public access. e.g. https://assets.your-domain.com"
        }
      },
      "required": [
        "bucketName",
        "baseUrl"
      ]
    }
  },
  "required": [
    "articleDir",
    "imageDir",
    "outputDir",
    "cacheDir",
    "categories",
    "tags"
  ],
  "additionalProperties": false
}
